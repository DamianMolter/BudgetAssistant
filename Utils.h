#pragma once
#include <iostream>
#include <string>
#include <math.h>

using namespace std;

class Utils{
public:
    static string loadLine();
    static string changeComaToDot(string input);
    static char loadCharacter();
};;
